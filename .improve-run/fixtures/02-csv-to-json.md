---
name: csv-to-json
description: Convert CSV files to JSON. Use when you need to transform CSV data into JSON format for an API.
---

# CSV to JSON

Read the CSV, parse the header row as keys, map each remaining row to an object, and emit a JSON array.
Handle quoted fields and commas inside quotes.
